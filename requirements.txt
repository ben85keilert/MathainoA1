# Laufzeit-Abhängigkeiten für Μαθαίνω A1.
# Installation: pip install -r requirements.txt
# (Alternativ: pip install -e . — nutzt dieselben Versionen aus pyproject.toml)
flet>=0.85,<0.86
pyttsx3>=2.99,<3
fpdf2>=2.8,<3
